#include <string.h>
#include "vaciarNumeros.h"

void vaciarNumeros(char* entrada){

    int w = strlen(entrada);
    int i;

    for(i = 0; i < w; i++){
        entrada[i] = '\0';
    }

    /*cy = 0;*/

}